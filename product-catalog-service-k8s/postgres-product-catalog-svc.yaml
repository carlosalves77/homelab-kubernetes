apiVersion: v1
kind: Service
metadata:
  name: postgres-product
  namespace: default
  labels:
    app: postgres-product
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None
  selector:
    app: postgres-product
