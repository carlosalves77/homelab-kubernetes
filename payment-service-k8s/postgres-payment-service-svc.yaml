apiVersion: v1
kind: Service
metadata:
  name: postgres-payment
  namespace: default
  labels:
    app: postgres-payment
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None
  selector:
    app: postgres-payment
